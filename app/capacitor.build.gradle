// Capacitor build configuration
// This file is automatically generated by Capacitor CLI

android {
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    
    // Ensure proper manifest merging
    sourceSets {
        main {
            java.srcDirs += 'src/main/java'
            res.srcDirs += 'src/main/res'
            assets.srcDirs += 'src/main/assets'
        }
    }
}

// Default Capacitor configuration - will be overridden by actual capacitor.build.gradle if it exists
